import {
  noop
} from "./chunk-YWUGNIHA.js";

// node_modules/svelte/src/internal/client/timing.js
var is_client = typeof window !== "undefined";
var request_animation_frame = is_client ? requestAnimationFrame : noop;
var now = is_client ? () => performance.now() : () => Date.now();
var raf = {
  tick: (
    /** @param {any} _ */
    (_) => request_animation_frame(_)
  ),
  now: () => now(),
  tasks: /* @__PURE__ */ new Set()
};

// node_modules/svelte/src/internal/client/loop.js
function run_tasks(now2) {
  raf.tasks.forEach((task) => {
    if (!task.c(now2)) {
      raf.tasks.delete(task);
      task.f();
    }
  });
  if (raf.tasks.size !== 0) {
    raf.tick(run_tasks);
  }
}
function loop(callback) {
  let task;
  if (raf.tasks.size === 0) {
    raf.tick(run_tasks);
  }
  return {
    promise: new Promise((fulfill) => {
      raf.tasks.add(task = { c: callback, f: fulfill });
    }),
    abort() {
      raf.tasks.delete(task);
    }
  };
}

export {
  raf,
  loop
};
//# sourceMappingURL=chunk-PMCBTWW3.js.map
